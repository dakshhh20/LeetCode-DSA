class Solution:
    def isHappy(self, n: int) -> bool:
        def helper(n):

            sum = 0
            while n > 0:
                digit = n % 10
                sum += digit * digit
                n = n // 10
        
            return sum

        slow = n
        fast = n

        while True:
            slow = helper(slow)
            fast = helper(helper(fast))

            if fast == 1:
                return True

            if slow == fast:
                return False
        
        
        
