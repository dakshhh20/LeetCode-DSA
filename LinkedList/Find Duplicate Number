class Solution:
    def findDuplicate(self, nums: List[int]) -> int:
        duplic = set()

        for num in nums:
            if num in duplic:
                return num
            duplic.add(num)
        return -1
